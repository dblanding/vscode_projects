#include <iostream>

using namespace std;

int main()
{
    int a(2.5);
    const long large;  // with a const, it must be initialized as it is declared
    int b = '?';
    char c('\'');
    char z(500);
    unsigned char ch = '\201';
    int big = 40000;
    unsigned size(40000);
    double he's(1.2E+5);  // illegal name: can't use '
    float val = 12345.12345;
    return 0;
}